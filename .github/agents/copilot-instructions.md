# tdd-agent-23112025 Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-24

## Active Technologies

- Rust 1.75+ (stable toolchain per `rust-toolchain.toml`) + `tokio`, `clap` for CLI, `anyhow`, workspace crates (001-tdd-agent-machine)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] cargo clippy

## Code Style

Rust 1.75+ (stable toolchain per `rust-toolchain.toml`): Follow standard conventions

## Recent Changes

- 001-tdd-agent-machine: Added Rust 1.75+ (stable toolchain per `rust-toolchain.toml`) + `tokio`, `clap` for CLI, `anyhow`, workspace crates

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
